<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Confirmation</title>
    <style>

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #180f0f63;
            background-image: url(/images/orderbackground.jpg);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .confirmation-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
        }

        .confirmation-box {
            background-color: #fff;
            padding: 50px;
            border-radius: 12px;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
            width: 100%;
        }

        .checkmark-icon {
            font-size: 72px;
            color: #39d353;
            margin-bottom: 20px;
        }

        .confirmation-box h1 {
            font-size: 24px;
            color: #333;
            margin-bottom: 10px;
        }

        .confirmation-box p {
            font-size: 18px;
            color: #666;
            margin-bottom: 20px;
        }

        .confirmation-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }

        .button {
            padding: 12px 24px;
            text-decoration: none;
            font-size: 16px;
            font-weight: bold;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            display: inline-block;
        }

        .check-button {
            background-color: #ffeb3b;
            color: #000;
        }

        .continue-button {
            background-color: #ff5722;
            color: #fff;
        }

        .button:hover {
            opacity: 0.8;
        }

    </style>
</head>
<body>
    <div class="confirmation-container">
        <div class="confirmation-box">
            <div class="checkmark-icon">
                &#10004;
            </div>
            <h1>Payment Received</h1>
            <p>Your Order Has Been Successfully Placed</p>
            <p>Thanks For Your Order</p>

            <div class="confirmation-buttons">
                <a href="/userorders" class="button check-button">Check Your Order</a>
                <a href="/" class="button continue-button">Continue Shopping</a>
            </div>
        </div>
    </div>
</body>
</html>
